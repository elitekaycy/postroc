COMPLETED:
- workspace naming uses unique id (workspace-{shortId})
- rename functionality and delete functionality in sidebar menus
- drag and rearrange functionality with visual drop zones
- array type supports comma-separated default values
- improved visibility of action buttons (plus signs)
- custom scrollbar design (6px, minimal)
- Dockerfile created (multi-stage build with standalone output)
- CLI tool plan created (see CLI_PLAN.md) - file-based approach
- Export/Import feature (sidebar > More > Export/Import)
  - Export workspace, project, or category as .postroc.json
  - Import with modes: as-new, merge, or replace
  - Sensitive data stripping option
- CLI Sync feature (sidebar footer > "Sync to CLI" button)
  - One-click sync all workspaces to local folder
  - Uses File System Access API for direct filesystem writes
  - First time: prompts to select folder (e.g. ~/.config/postroc/data/)
  - Subsequent syncs: writes directly to saved location
  - Directory handle persisted in IndexedDB


TODO:
- CI/CD GitHub pipeline to build Docker image
- Pipeline to pull from Docker and deploy to server
- Implement CLI tool (separate project - postroc-cli)
- (Optional) Add lightweight backend for collaboration features


NOTES:
- domain: postroc.scalestash.com (landing) / app.postroc.scalestash.com (app)
- contact: contact@scalestash.com
- CLI plan: CLI_PLAN.md (updated to file-based, no database required)
- Current data storage: localStorage (client-side only)
- CLI approach: Sync to CLI button writes to ~/.config/postroc/data/
- For team collaboration/sync, can add Supabase/Firebase later (optional)


STANDALONE PROJECTS:
- postroc-cli (../postroc-cli/)
  - CLI tool for generating mock data from PostRoc configs
  - Commands: generate, list, import, config
  - Reads from ~/.config/postroc/data/*.postroc.json
  - npm install -g postroc-cli

- postroc-landing (../postroc-landing/)
  - Marketing landing page
  - Static Next.js site (output: export)
  - Deploy to postroc.scalestash.com
